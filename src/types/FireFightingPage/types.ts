export type fireFightingPropsType = {
  // header: string;
  // secondHeader: string;
  // mainPra: string;
  // secondPra?: string;
  location: string;
};
export type secondSectionPropsType = {
  header: string;
  pra: string;
  button: string;
  to: string;
  pageName: string;
};
