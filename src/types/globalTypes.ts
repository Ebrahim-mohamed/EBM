export type heroType = {
  header: string;
  location: string;
  pra?: string;
  button?: string;
  isCenter?: boolean;
};
export type headerType = {
  header: string;
  pra?: string;
  pageName: string;
  sectionName: string;
  isNotCenter: boolean;
};
export type endSectionType = {
  header: string;
  button: string;
  to: string;
  pageName: string;
};
